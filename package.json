{
  "name": "smartwatch-checkout-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm test --prefix backend && npm test --prefix frontend",
    "test:cov": "npm run test:cov --prefix backend && npm run test:cov --prefix frontend",
    "backend:dev": "npm run start:dev --prefix backend",
    "backend:build": "npm run build --prefix backend",
    "backend:start": "npm run start:prod --prefix backend",
    "frontend:dev": "npm run dev --prefix frontend",
    "frontend:build": "npm run build --prefix frontend"
  }
}
